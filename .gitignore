# Secrets — the hot-wallet key and any deploy credentials live in .env.
# Never commit these.
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Runtime data (hit log, agent datastore sqlite)
data/

# Generated caches
x402.webbersites.com/docs/.openapi-cache.json

# OS / editor noise
.DS_Store
*.log

# The MCP npm package is its own repo: webberdesign/webbersites-x402-mcp
mcp/

# local link file
Claude.webloc

# Filled-in admin panel credentials live only locally + on GoDaddy.
# The placeholder template is admin.php.example.
x402.webbersites.com/manage/admin.php
